<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyApplication3" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/materialGreen</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/materialGrey_800</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.MyApplication3.NoActionBar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- theme for collapsable Action bar text -->
    <style name="myTheme.ActionBar.Text" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="android:textColor">@color/materialGreen_Deep</item>
        <!-- Sets the action bar text colour -->
        <item name="android:textColorPrimary">@color/primary_colour</item>
        <item name="colorPrimaryDark">@color/materialGrey_800</item>
    </style>

    <!-- theme for switch Switch -->
    <style name="Theme.MyApplication3.Switch" parent="Theme.AppCompat.DayNight">
        <item name="colorControlActivated">@color/purple_200</item>
        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">#FFE1E1E1</item>
        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">#FF8E8E8E</item>
    </style>

</resources>